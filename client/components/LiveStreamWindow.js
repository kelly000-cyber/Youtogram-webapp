export default function LiveStreamWindow() {
  return (
    <div style={{ border: '1px solid #ddd', borderRadius: 8, padding: '1rem' }}>
      <h2>Live Stream</h2>
      <div style={{ background: '#000', color: '#fff', minHeight: '220px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        Live streaming placeholder
      </div>
    </div>
  );
}
